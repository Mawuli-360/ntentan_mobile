// import 'package:ntentan/core/db/app_database.dart';

class DatabaseStorage {
  // final AppDatabase _db;

  // DatabaseStorage({required AppDatabase db}) : _db = db;

  // Example method to interact with the database
  // Future<List<User>> getAllUsers() async {
  //   return await _db.select(_db.users).get();
  // }

  // Future<int> addUser(UsersCompanion user) async {
  //   return await _db.into(_db.users).insert(user);
  // }

  // Future<void> deleteUser(int id) async {
  //   await (_db.delete(_db.users)..where((t) => t.id.equals(id))).go();
  // }
}
